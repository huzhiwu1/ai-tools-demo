/**
 * 修复提示词 —— 用于 WorkflowRepairer
 *
 * 职责：引导 LLM 分析错误信息，修复工作流中的问题
 *
 * 流程：
 * 1. 接收 CozeWorkflow + 错误信息
 * 2. 分析错误原因，定位问题节点
 * 3. 生成修复方案
 * 4. 应用修复并重新校验
 *
 * 关键细节：
 * - 错误分为两类：
 *   - 校验错误：本地校验发现的结构问题
 *   - 平台错误：Coze API 返回的执行错误
 * - 修复策略因错误类型不同而异
 * - 必须记录修复历史，防止重复修复
 * - 最大修复轮次为 3，超过则报告无法自动修复
 *
 * TODO: 后续优化
 * - 添加常见错误和修复方案的映射表
 * - 添加 Coze 平台错误码说明
 * - 添加修复历史追踪
 */
export const REPAIR_PROMPT = `你是一个 Coze 工作流修复专家。请分析以下错误信息，修复工作流中的问题。

## 出错的 Coze 工作流
{workflow}

## 错误信息
{errors}

## 常见错误和修复方法
1. 节点 ID 重复 → 为每个节点生成唯一 ID
2. 边引用了不存在的节点 → 检查边的 source/target 是否正确
3. 缺少 start 节点 → 在工作流开头添加 start 节点
4. 缺少 end 节点 → 在工作流末尾添加 end 节点
5. LLM 节点缺少 userPrompt → 为用户提示词添加内容
6. 条件节点分支不足 → 确保至少有两个分支
7. 变量引用格式错误 → 使用 {{变量名}} 格式

## 要求
1. 分析每个错误的原因
2. 定位问题节点和边
3. 生成修复方案
4. 输出修复后的完整工作流 JSON

## 输出格式
请以 JSON 格式返回修复后的完整 Coze 工作流。`;

// TODO: 后续补充 Coze 平台错误码和修复方案
